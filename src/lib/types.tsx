export interface TermsAndConditionsProps {
  opened: boolean;
  close: () => void;
}

export interface commentProps {
  name: string;
  rating: number;
  details: string;
}

export interface footerProps {
  year: string;
  fullName: string;
  studentId: string;
}